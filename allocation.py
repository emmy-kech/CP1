class Space_allocation(object):
	# 'common base class for space allocation'
	def __init__(self): 
		self.office=offices
		self.livingspace=livingspace
		occupied_room=[]
		total_space = 10

	def allocate_room(self,offices,livingspace):
		if len(occupied_room) < self.total_space:
			occupied_room+self.offices and self.livingspace
			return occupied_room
		if len(occupied_room) == self.total_space:
			return "All room spaces are occupied"

print(occupied_room)


